services:
  - type: web
    name: portfolio
    runtime: python311
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
      - key: PORT
        value: 10000

databases:
  - name: postgres
    plan: free

